# Golden test cases with score < 0.5

Cases to fix one by one. Sorted by score ascending (worst first).

Generated by: `python3 tools/golden_compare/compare_golden.py --verbose`, filtered for `score < 0.5`.

**Total: 45 cases**

---

## Checklist

After fixing a case, re-run and update golden output:

```bash
./scripts/update_golden_output.sh
python3 tools/golden_compare/compare_golden.py --threshold 0.30
```

Then mark the row below with `[x]`.

---

| # | test_case | score | formula | fixed |
|---|-----------|-------|---------|-------|
| 1 | 0198 | 0.330 | `\xrightarrow{x}` | [x] |
| 2 | 0063 | 0.350 | `\xrightarrow{f}` | [x] |
| 3 | 0077 | 0.360 | `\begin{alignedat}{2} x &= 1 & y &= 2 \\ a &= 3 & b &= 4 \end{alignedat}` | [ ] |
| 4 | 0356 | 0.382 | `\xhookrightarrow{text}` | [ ] |
| 5 | 0355 | 0.392 | `\xhookleftarrow{text}` | [ ] |
| 6 | 0294 | 0.393 | `\intop_0^1 f` | [ ] |
| 7 | 0360 | 0.398 | `\xleftharpoondown{text}` | [ ] |
| 8 | 0357 | 0.400 | `\xmapsto{text}` | [ ] |
| 9 | 0353 | 0.407 | `\xleftrightarrow{text}` | [ ] |
| 10 | 0368 | 0.407 | `\xrightleftarrows{text}` | [ ] |
| 11 | 0414 | 0.408 | `\varinjlim` | [ ] |
| 12 | 0358 | 0.411 | `\xrightharpoondown{text}` | [ ] |
| 13 | 0200 | 0.412 | `\overbrace{x}^2` | [ ] |
| 14 | 0415 | 0.413 | `\varprojlim` | [ ] |
| 15 | 0191 | 0.414 | `\begin{smallmatrix} a & b \\ c & d \end{smallmatrix}` | [ ] |
| 16 | 0231 | 0.415 | `\begin{aligned}[a]&[b]\\ [c]&[d]\end{aligned}` | [ ] |
| 17 | 0161 | 0.429 | `\int\limits_2^2 3` | [ ] |
| 18 | 0249 | 0.429 | `\underleftarrow{AB}` | [ ] |
| 19 | 0250 | 0.429 | `\underleftrightarrow{AB}` | [ ] |
| 20 | 0230 | 0.432 | `f(a,b)=\begin{cases}a+1&\text{if }b\text{ is odd}\\a&\text{if }b=0\\a-1&\text{otherwise}\end{cases}` | [ ] |
| 21 | 0102 | 0.437 | `\sqrt[\small 3]{x}` | [ ] |
| 22 | 0196 | 0.439 | `\overrightarrow{x}` | [ ] |
| 23 | 0143 | 0.440 | `\rule{-1em}{- 0.2em}` | [ ] |
| 24 | 0148 | 0.440 | `\kern-1em` | [ ] |
| 25 | 0354 | 0.443 | `\xLeftrightarrow{text}` | [ ] |
| 26 | 0086 | 0.450 | `+-*\cdot\pm\div` | [ ] |
| 27 | 0351 | 0.456 | `\xLeftarrow{text}` | [ ] |
| 28 | 0134 | 0.460 | `\text{a~ b}` | [ ] |
| 29 | 0352 | 0.460 | `\xRightarrow{text}` | [ ] |
| 30 | 0126 | 0.463 | `\text{a \vdots b ⋮ d}` | [ ] |
| 31 | 0363 | 0.463 | `\xleftrightharpoons{text}` | [ ] |
| 32 | 0091 | 0.468 | `,;` | [ ] |
| 33 | 0053 | 0.470 | `\begin{aligned} x &= 1 \\ y &= 2 \end{aligned}` | [ ] |
| 34 | 0078 | 0.470 | `\begin{alignedat}{1} x &= 1 \\ y &= 2 \end{alignedat}` | [ ] |
| 35 | 0330 | 0.470 | `x` | [ ] |
| 36 | 0362 | 0.473 | `\xrightleftharpoons{text}` | [ ] |
| 37 | 0020 | 0.477 | `\widehat{ABC}` | [ ] |
| 38 | 0021 | 0.478 | `\bar{z}` | [ ] |
| 39 | 0062 | 0.483 | `\underbrace{x+y}` | [ ] |
| 40 | 0207 | 0.484 | `\operatorname*{x*Π∑\Pi\sum\frac a b}_y x` | [ ] |
| 41 | 0151 | 0.486 | `\dfrac{y}{z} \right)` | [ ] |
| 42 | 0049 | 0.487 | `\begin{vmatrix} a & b \\ c & d \end{vmatrix}` | [ ] |
| 43 | 0201 | 0.488 | `\underbrace{x}_2` | [ ] |
| 44 | 0206 | 0.493 | `\operatorname{x*Π∑\Pi\sum\frac a b}` | [ ] |
| 45 | 0163 | 0.499 | `\int_2^2\limits` | [ ] |

---

## SOP: Fixing Low-Score KaTeX Stretchy Arrow Cases

Applies to `\xrightarrow`, `\xleftarrow`, `\xtwoheadrightarrow`, `\xtwoheadleftarrow`, `\xmapsto`, and similar commands.

### 1. Find KaTeX SVG Path Data

```bash
grep -A2 "xmapsto\|leftmapsto\|rightarrow" \
  tools/golden_compare/node_modules/katex/dist/katex.js | head -20
```

Key fields:
- Single-headed arrows (`rightarrow`, `leftarrow`): viewBox `400000 × 534`, center line `vb_cy = 261`
- Double-headed arrows (`twoheadrightarrow`, `twoheadleftarrow`): viewBox `400000 × 334`, center line `vb_cy = 167`
- Compound commands (e.g. `xmapsto`) use the format `[["leftmapsto", "rightarrow"], min_width, vb_height]`

### 2. Coordinate Transform Rules

Uniform scale: `s = height_em / vb_height`

```
x_new = x_vb * s + x_shift
y_new = (y_vb - vb_cy) * s
```

**x_shift rules**:
- Right-pointing arrow (tip at right): `x_shift = width_em - 400000 * s`
- Left-pointing arrow (tip at left, x≈0): `x_shift = 0.0`

### 3. Implementation Steps (in `katex_stretchy_arrow_path` in `katex_svg.rs`)

1. Locate the path string in `katex.js` and add it as a `const`
2. Parse with `parse_svg_path()`
3. Apply coordinate transform with `scale_cmd_twohead_uniform(cmd, s, vb_cy, x_shift)`
4. Clip to render region with `clip_path_to_rect(&cmds, 0.0, width_em, y_min, y_max)`
5. For compound commands (e.g. `\xmapsto`), process each path segment separately then merge (`combined.extend(...)`)

### 4. Common Pitfalls

| Issue | Cause | Fix |
|-------|-------|-----|
| Left arrow head disappears | Negative `x_shift` places head outside clip region | Use `x_shift = 0.0` for left arrows; the head is already near x≈0 in the viewBox |
| Shaft segments have color gaps / path not closed | `clip_path_to_rect` skips the clip boundary start point when a gap is detected | When detecting a gap, emit `LineTo(a)` before `LineTo(b)` |
| Closing edge missing | Loop uses `0..n-1`, missing the last edge from p[n-1] back to p[0] | Change to `for i in 0..n`, use `contour[(i+1) % n]` for the next point |
| Shaft too thin to be visible | `arrow_h` is too small; shaft thickness `40 * s` < 1px | Increase `arrow_h` or compensate with stroke width |
| Path reflection logic wrong | Extra x-reflection applied to left-pointing arrows | Use left-pointing path constants directly (e.g. `LEFTARROW`), no reflection needed |

### 5. Verification

```bash
# Render a single case (line N)
sed -n 'Np' tests/golden/test_cases.txt | \
  cargo run --release -p ratex-render --bin render -- \
  --font-dir tools/lexer_compare/node_modules/katex/dist/fonts \
  --output-dir /tmp/ratex_test/

# Update golden outputs and run comparison
./scripts/update_golden_output.sh
python3 tools/golden_compare/compare_golden.py --threshold 0.30
```

---

## Regenerate this list

To refresh the list after layout/rendering changes:

```bash
python3 tools/golden_compare/compare_golden.py --verbose 2>&1 | tee /tmp/golden_verbose.txt
# Then filter for score < 0.5 (or run a script to regenerate this file)
```
